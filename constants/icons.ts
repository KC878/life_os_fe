import { BrickWallFire, Camera, GalleryThumbnails, ListTodo, SquarePen } from 'lucide-react';


export const icons = {
  gallery: GalleryThumbnails,
  fire: BrickWallFire,
  todo: ListTodo,
  note: SquarePen,
  camera: Camera,
}